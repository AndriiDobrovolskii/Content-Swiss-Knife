---
artifact: ac_test_matrix
story: US-2.2
version: 3
status: ARCHIVED
owner: so-test-writer
created_at: 2026-09-21T20:30:00Z
updated_at: 2026-09-21T23:30:00Z
supersedes: docs/tests/US-2.2-ac-test-matrix.md#2
inputs_consumed:
  - key: story
    version: 1
  - key: specification
    version: 1
  - key: implementation_plan
    version: 2
  - key: task_breakdown
    version: 2
  - key: test_strategy
    version: 1
open_decisions_blocking: false
---

# AC <-> Test Matrix — US-2.2

Built from the test names REGISTERED by `vitest run --reporter=json` (see the generation report), not from file contents. Ids are the Story's `AC-n` verbatim (the Story lists AC-11..AC-16 out of order; all sixteen are present). A test named here that is currently green is marked `guard`: it pins existing behaviour that must survive and is not evidence for the new behaviour. `[module missing]` means the file registers no tests until the named task creates the module it imports; the test names below are taken from the spec files.

Runner: all files are `test:logic` (`vitest run`) except AC-1/AC-2/AC-9 component rows marked `test:components`.

| AC | Test file | Test (describe > test) | Task that turns it green |
|---|---|---|---|
| **AC-1** four options, order, no empty option, Full default, both forms | `src/app/components/content-template-select/content-template-select.component.spec.ts` (`test:components`) | "AC-1: exactly four options, in order, Full selected" > "lists Full description, Filaments/resins/powders, Accessories, Spare parts in that order (EN)", "has exactly four options: no empty \"Select Template...\" option", "option values are the static empty string for Full, then the three registry ids", "\"Full description\" is selected by default" [module missing] | T14 |
| AC-1 (both forms) | `src/app/app.component.template-wiring.spec.ts` | "AC-1 — both forms use the one dropdown component; the old markup is gone" > "renders the dropdown component exactly twice: Generator form and SEO-only form", "no inline \"Select Template...\" option remains in the parent template" (guard part) | T14 |
| AC-1 (list source) | `src/app/content-template-labels.spec.ts` | "AC-1 / AC-2 — CONTENT_TEMPLATES holds the three simplified entries and no consumables option" > "has exactly the three registry ids, in order", "Full description is not an entry" [module missing] | T1, T14 |
| **AC-2** EN/UA labels from `uiLabels`, no consumables option | `src/app/content-template-labels.spec.ts` | "AC-2 — the label module" > "EN renders \"Full description / Filaments, resins, powders / Accessories / Spare parts\"", "UA renders \"Повний опис / Філаменти, смоли, порошки / Аксесуари / Запчастини\"", "EN and UA carry exactly the same key set…", "every key is a non-empty string that is not the raw key" (en, uk), "the removed consumables label key is gone" [module missing] | T14 |
| AC-2 (wiring) | `src/app/app.component.template-wiring.spec.ts` / `src/app/content-template-labels.spec.ts` | "AC-2 — app.component wires the labels into uiLabels and drops the old key" > "spreads TEMPLATE_LABELS.en and TEMPLATE_LABELS.uk…", "no longer defines consumablesTemplateName" | T14 |
| AC-2 (rendered) | `content-template-select.component.spec.ts` (`test:components`) | "AC-2: labels come from the uiLabels map, EN and UA" > "renders the Ukrainian labels when given the UA label map", "never shows the removed consumables option (EN/UA)" [module missing] | T14 |
| **AC-3** Filaments: 1,2,4,5,7,8; no §3, §6, §9 (Doc path) | `src/prompts/task-a-doc.simplified.spec.ts` | "AC-3 — Filaments/resins/powders requests 1, 2, 4, 5, 7, 8 and no §3, §6, §9" > "requests §1/2/4/5/7/8" (guard), "does not request §3/§6/§9", "the JSON shape lists no \"functionality\"/\"packageContents\"/\"faq\" key" | T8 |
| AC-3 (legacy path) | `src/prompts/task-a.simplified.spec.ts` | "FR-7 — the legacy path requests only the template paragraphs" > "Filaments: 1, 2, 4, 5, 7, 8 requested; 3, 6, 9 not" | T9 |
| AC-3 (registry) | `src/prompt-core/simplified-templates.spec.ts` | "paragraph sets (AC-3, AC-4, AC-5)" > "Filaments/resins/powders requests 1, 2, 4, 5, 7, 8 and never 3, 6, 9", "no simplified template ever requests §6 or §9" [module missing] | T1 |
| AC-3 (orchestrator) | `src/services/content-orchestrator.simplified.spec.ts` | "generate() on a Doc-pipeline store…" > "Filaments with non-empty specs: prompt asks for §7; the render has ONE <tbody>, no <h3> anywhere" | T12 |
| AC-3 (template completeness) | `src/domain/description-doc.completeness.spec.ts` | "Filaments/resins/powders (AC-3, AC-8, AC-15)" > "missing required killerSpecs/keyBenefits/applications is an error", "a stray §3 functionality is a warning…", "a stray §6 packageContents is a warning…" [module missing] | T5 |
| **AC-4** Accessories: 1,2,3(flag),5,7,8; no §4, §6, §9 | `src/prompts/task-a-doc.simplified.spec.ts` | "AC-4 — Accessories requests 1, 2, 5, 7, 8 (+3 with the checkbox) and no §4, §6, §9" > "checkbox checked: requests 1, 2, 3, 5, 7, 8 and not 4, 6, 9", "checkbox unchecked: requests 1, 2, 5, 7, 8 and not 3, 4, 6, 9 in the per-run text" | T8 |
| AC-4 (legacy) | `src/prompts/task-a.simplified.spec.ts` | "FR-7 …" > "Accessories checked: 1, 2, 3, 5, 7, 8 requested; 4, 6, 9 not", "Accessories unchecked: no §3, and a missing flag equals unchecked" | T9 |
| AC-4 (registry, completeness) | `simplified-templates.spec.ts`; `description-doc.completeness.spec.ts` | "Accessories with the flag checked requests 1, 2, 3, 5, 7, 8…"; "Accessories (AC-4, AC-9)" > "flag checked: a document without §3 is an error at functionality", "a stray §4 applications is a warning, never an error" [module missing] | T1, T5 |
| **AC-5** Spare parts: only 1, 5, 8 | `src/prompts/task-a-doc.simplified.spec.ts` | "AC-5 — Spare parts requests only 1, 5, 8" > "requests §1/§5/§8", "does not request §2/§3/§4/§6/§7/§9", "the JSON shape lists no \"killerSpecs\"/…/\"faq\" key" | T8 |
| AC-5 (legacy, orchestrator, registry, completeness) | `task-a.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts`; `simplified-templates.spec.ts`; `description-doc.completeness.spec.ts` | "Spare parts: only 1, 5, 8"; "Spare parts: the master is requested as a Doc with paragraphs 1, 5, 8 only, and renders no specs section"; "Spare parts requests only 1, 5, 8 whatever the flag says"; "Spare parts (AC-5)" > "a stray killerSpecs/applications/specs/functionality/packageContents is a warning, not an error" | T9, T12, T1, T5 |
| **AC-6** Full byte-identical | `src/prompts/full-description.golden.spec.ts` (guard, green today) | "Full description prompts are byte-identical to the pre-Story goldens (FR-6, NFR-6)" > one test per case: `doc/expert3d`, `doc/expert3d+hook`, `doc/c3d`, `html/expert3d`, `html/legacy`, `html/legacy+lang`, `html/c3d+customTemplate`, `translate/uk-user`, `translate/de-internal`, `c/expert3d-es`, `c/eu-en`, `c/us-uk`, plus "is a pure function of its input" | T1 (first green), stays green through T17 |
| AC-6 (id handling) | `task-a-doc.simplified.spec.ts`; `task-a.simplified.spec.ts`; `task-translate.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "FR-6 / NFR-6 — Full description and determinism" (empty-string id equals Full; stale id builds the Full block); "FR-6 — Full description on the legacy path"; "undefined, empty and unknown ids add nothing (Full description is byte-identical)"; "Full description (no templateId) requests the unchanged Full task block" (guard) | T8, T9, T12 |
| **AC-7** single-table §7, no `<h3>`, prompt states it | `src/render/render-description.flat-specs.spec.ts` | "flatSpecs with one category (Filaments, Accessories)" > "exactly one <tbody>, no <h3>, and the category title is not rendered", "keeps the Full §7 markup — section.specs, its <h2>, table-responsive, table table-bordered table-striped", "every parameter row is present, in order, inside the single <tbody>"; "flatSpecs with several categories" > "drops no row: all 9 rows appear, in category order"; "Full description is unchanged by the new option" | T4 |
| AC-7 (prompt states it) | `task-a-doc.simplified.spec.ts`; `task-a.simplified.spec.ts` | "AC-7 — single-table §7 is stated explicitly" > "one <tbody>, no <h3> sub-headings, one category" (filaments, accessories); "FR-3..FR-5 / FR-10 / FR-11 …" > "single-table §7, one <tbody>, no <h3>" | T8, T9 |
| AC-7 (enforcement) | `src/domain/description-doc.completeness.spec.ts`; `src/utils/simplified-specs-shape.spec.ts`; `src/utils/output-validator.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "FR-8 on the Doc path — rule simplified-specs-shape" > "two categories is an error at specs.categories saying \"exactly one category, no headings\"", "empty categories with a present specs is an error"; "FR-8 violations are errors, per shape" > two <tbody>, <h3>, category title row, two tables, pre-flat renderer output; "FR-8 through validateGeneratedHtml — non-flat §7 is rejected for simplified ids only"; "FR-8: a two-category simplified §7 triggers a retry naming \"exactly one category\"; the accepted render is flat" [module missing for the first two] | T5, T6a, T10, T12 |
| AC-7 (cross-store) | `test/render-conformance.simplified.spec.ts` | "conformance — $template — $storeName / $locale" > "AC-7: one <section class=\"specs\"> with ONE <tbody>, no <h3>, followed by the single <hr>" (20 pairs x filaments, accessories) [module missing] | T3, T4, T5 |
| **AC-8** §5 only with compatibility data | `task-a-doc.simplified.spec.ts`; `task-a.simplified.spec.ts` | "AC-8 — §5 is emitted only when the source carries compatibility data" > "%s: the §5 instruction is conditional on source data" (guard on Doc path); legacy: "%s: §5 only when the source has compatibility data" | T8, T9 |
| AC-8 (data-conditional in doc/registry) | `description-doc.completeness.spec.ts`; `description-doc.schema.simplified.spec.ts` | "a valid document (with and without §5) yields no issues"; "accepts a document that omits §5 compatibility" (guard) | T3, T5 |
| **AC-9** checkbox visible only for Accessories; §3 only when checked | `content-template-select.component.spec.ts` (`test:components`) | "AC-9: the \"Include Functionality (§3)\" checkbox" > "is absent unless the parent asks for it…", "is visible with the EN label when shown", "is visible with the UA label when shown in Ukrainian", "is unchecked by default", "clicking it emits functionalityChange(true)", "unchecking it emits functionalityChange(false)" [module missing] | T14 |
| AC-9 (parent wiring) | `src/app/app.component.template-wiring.spec.ts` | "AC-9 — the checkbox is a Generator-form-only control bound to Accessories" > "exactly one of the two usages enables the functionality toggle, and its condition names accessories", "the SEO-only usage never binds the toggle or the flag"; "OD-6 …" > default false, reset on template change, not persisted | T14 |
| AC-9 (prompt) | `task-a-doc.simplified.spec.ts`; `task-a.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "AC-9: the checked and unchecked prompts differ, and only in userContent (NFR-1)"; "AC-9: the per-run text asks for §3 when checked and does not when unchecked"; "the flag is ignored for any other template"; legacy "Accessories: the checkbox flag reaches the legacy prompt"; orchestrator "Accessories with the checkbox %s (%s): the flag reaches the prompt (AC-9)" | T2, T8, T9, T12 |
| **AC-10** shared v4 rules: hook `<b>name</b> —`, CTA `<p>` under localized H2, no `<h1>` | `description-doc.schema.simplified.spec.ts`; `render-description.simplified.spec.ts`; `test/render-conformance.simplified.spec.ts` | "still enforces the v4 hook invariant start on a simplified document (AC-10 / FR-12)"; "FR-18 — §1 + §8 only" > "the CTA heading is the localized \"Чому … в …?\" form…", "contains no <h1>"; conformance "AC-10: the hook is a single <p> opening with <b>{name}</b> —, and the body has no <h1>", "AC-10: the CTA is a <p> under the localized … heading for this store and locale" (20 pairs x 3 templates) [conformance: module missing] | T3, T5 |
| **AC-11** schema accepts omitted/null §2,3,4,6,7,9; rejects missing §1/§8 | `src/domain/description-doc.schema.simplified.spec.ts` | "FR-17 — optional paragraphs (omitted or null)" > "accepts a document that omits/sets to null §2, §3, §4, §5, §6, §7" (12 tests), "accepts §2, §3, §4, §6 and §7 all omitted at once", "accepts a document with no §9 FAQ field…"; "FR-17 — §1 and §8 stay required" > "rejects a document missing §1 (hook), at path \"hook\"", "rejects a document missing §8 (cta)…", "rejects a null hook and a null cta" | T3 |
| AC-11 (bounds, cross-field, Full) | same file | "bounds still apply whenever a present parent field is present (OD-13c)"; "cross-field checks skip when their target fields are absent (OD-13c)"; "existing Full-description documents validate as before" (corpus fixtures, guard) | T3 |
| AC-11 (renderer + walkers) | `render-description.simplified.spec.ts`; `src/utils/doc-absent-paragraphs.spec.ts` | "FR-18 — every combination of absent paragraphs renders without throwing or stray markup" > "all 64 combinations (omitted/null)…"; one describe per walker: doc-block-repair, doc-tier, heading-style, sentence-length, tov-second-person, bullet-lead-punctuation, alt-numeric-fidelity, video-manifest, specs-grounding, spec-count-parity, spec-category-shape (mandatory per-walker case, T3/N2) | T3 |
| **AC-12** v4 wins over legacy limits | `src/prompts/task-a-doc.simplified.spec.ts` | "AC-12 — v4 ranges in every simplified prompt; no legacy consumables limit" > "%s: names the v4 ranges of the paragraphs it includes and none of an excluded one", "%s: carries no legacy consumables limit or consumables wording", "Filaments: applications 4-8 items; Killer Specs block at most 8 items" | T8 |
| AC-12 (legacy, registry) | `task-a.simplified.spec.ts`; `simplified-templates.spec.ts` | "FR-13 / FR-15 — v4 ranges and the soft ceiling on the legacy path" > "%s: ranges of included paragraphs only, no legacy limit, soft 5500 ceiling"; "v4 word ranges and the soft ceiling" > "encodes the v4 ranges, minimum and maximum", "no legacy consumables limit survives in the table" [module missing] | T9, T1 |
| **AC-13** consumables removed, nothing references it | `test/removal.spec.ts` | "AC-13 / FR-20 — removed files and directories" > "<path> no longer exists" (15 paths); "no source, spec or fixture references a removed item" > "the removed template id, doc-pipeline literal, domain type, renderer and flag are referenced nowhere (green at T11)", "no CONSUMABLES_ constant, overlay or pipeline-enablement name is referenced anywhere (green at T13)" | T11, T13 |
| AC-13 (exports, routing, overlay) | `doc-pipeline-flag.simplified.spec.ts`; `output-validator.simplified.spec.ts`; `task-translate.simplified.spec.ts`; `spec-category-shape.simplified.spec.ts` | "FR-20 — the consumables exclusion and flag are removed"; "FR-13 — the legacy consumables character-limit gate is gone"; "no template id, and a stale template id, produce the same prompt (overlay is gone)"; "a stale template id … behaves as Full" [flag spec: module missing] | T10, T11, T13, T9 |
| **AC-14** 5500 soft ceiling stated; v4 ranges win; Full unaffected | `task-a-doc.simplified.spec.ts` | "AC-14 — 5500-character soft ceiling, v4 ranges win" > "%s: states a 5500 soft ceiling on narrative text and that the v4 ranges take priority" (3 templates), "the ceiling is not stated as a hard limit", "Full description prompt does not mention the ceiling (AC-14: Full is unaffected)" | T8 |
| AC-14 (legacy, translation) | `task-a.simplified.spec.ts`; `task-translate.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "FR-13 / FR-15 …" (soft 5500 ceiling, priority, exceed); "buildTranslatePrompt (Doc pipeline) — FR-19" > "carries the preserve-omissions clause" (no length limit, no 5500); "FR-19 wiring — every translation call carries the preserve-omissions clause…" | T9, T8, T12 |
| **AC-15** empty specs -> no §7; non-empty -> §7 | `task-a-doc.simplified.spec.ts` | "AC-15 — §7 is omitted when the source specs are empty, present when they are not" > "%s: empty specs -> the per-run text omits §7", "%s: non-empty specs -> §7 is requested and not negated per run" (guard), "the empty-specs signal never enters a cached system block (NFR-1)", "whitespace-only specs count as empty" | T8 |
| AC-15 (legacy, gate, orchestrator) | `task-a.simplified.spec.ts`; `description-doc.completeness.spec.ts`; `spec-count-parity.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "%s: empty specs -> §7 omitted; non-empty -> not negated"; "hasSpecs true and §7 absent is an error at specs", "hasSpecs false and §7 absent is fine", "hasSpecs false and §7 present is flagged at specs"; "Doc variant" > "§7 absent -> no throw, no issue"; "Filaments with EMPTY specs: the per-run prompt omits §7 and the render has no specs table or heading (AC-15)" | T8, T9, T5, T7, T12 |
| **AC-16** ranges are hard, ceiling soft | `src/utils/simplified-word-ranges.spec.ts` | "FR-14 — every range, minimum and maximum, on the Doc entry point" (21 cases); "…on the HTML entry point" (21); "V6 — Doc and HTML entry points agree on every shared fixture"; "AC-16 — the hard rule beats total length; the ceiling is soft" > "a description under 4000 characters with a 120-word hook is rejected (the Story's own example)", "a narrative above 5500 characters with every paragraph inside its range is accepted", "the ceiling produces no issue of any severity…", "a document over 5500 characters that ALSO breaks a range is rejected…"; scope tests (present paragraphs only, uk-UA master only, Full skipped) [module missing] | T6 |
| AC-16 (frozen entry point, orchestrator) | `src/utils/output-validator.simplified.spec.ts`; `content-orchestrator.simplified.spec.ts` | "FR-14 / AC-16 through validateGeneratedHtml" > "a 120-word hook adds an error under a simplified id, at any length", "a narrative above 5500 characters with every paragraph in range adds NO error (the ceiling is soft)", "the same over-range document adds nothing under Full description", "a translated locale is not range-checked"; orchestrator "FR-14: a 120-word hook triggers a retry that states the 40–85 range…", "FR-16: a >5500-character narrative with every paragraph in range is NOT retried", legacy "FR-14 on the legacy path…" | T10, T12 |

## Requirement traceability beyond the ACs

| Requirement | Test file(s) |
|---|---|
| FR-7 both generation paths | `doc-pipeline-flag.simplified.spec.ts` (routing per store), `content-orchestrator.simplified.spec.ts` (Doc store + Expert-3DPrinter) |
| FR-19 preserve master omissions | `task-translate.simplified.spec.ts`, `simplified-translation-parity.spec.ts`, orchestrator "FR-19 wiring" |
| FR-21..FR-25 (AGENTS.md §4 media, units, meta, itemtype) | `test/render-conformance.simplified.spec.ts`, `simplified-media-survival.spec.ts`; FR-24 not newly tested (strategy) |
| NFR-1 cache-block separation | `task-a-doc.simplified.spec.ts` "NFR-1", `task-a.simplified.spec.ts` "NFR-1" |
| NFR-6 determinism | `full-description.golden.spec.ts`, "is deterministic: two builds of the same simplified input are byte-identical" |
| D11 four-template agreement | `simplified-templates.consistency.spec.ts` [module missing] |
| R6 FAQ data-driven | `content-orchestrator.simplified.spec.ts` "R6 (human resolution)" (11 tests + UA-only mode 3) |
| D14 stale id degrades to Full | registry, completeness, ranges, flag, prompt specs (each uses `STALE_TEMPLATE_ID` from `test/fixtures/removed-tokens.ts`) |

Open Decisions that leave a test intentionally loose: OD-5 (Full identity: tests assume `templateId` undefined/empty), OD-6 (checkbox default unchecked, reset on template change, not persisted: taken from the plan), OD-7 (Spare parts §5 with no compatibility data: only "§5 is requested" is asserted, no claim about absence), OD-8 (no assertion on which v4 sentence is cited), OD-10 (label strings taken from FR-2), OD-15 (range mapping onto repair: tests assert `error` severity and retry only).

## Revision 3 - supplementary rows

| Requirement | Test |
|---|---|
| FR-18 absent paragraph skipped, `'3.0'` | `render-description.branches.spec.ts` "'3.0' §2 with only one half present (FR-18)" (4 tests) |
| FR-18 absent paragraph skipped, `'4.0'` | `render-description.branches.spec.ts` "'4.0' §2 combined list with one half absent" (4 tests) |
| FR-8 flat §7 multi-valued row | `render-description.branches.spec.ts` "flatSpecs with a multi-valued SpecRow (FR-8)" (2 tests) |
| Doc-branch failure text reaches the repair prompt | `doc-schema-issues.branches.spec.ts` (8 tests) |
